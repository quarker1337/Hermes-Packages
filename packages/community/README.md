# Community packages

Community packages are not enabled by default yet.

Initial policy:
- official packages only in the default source
- no arbitrary post-install scripts
- explicit permissions in every manifest
- signed indexes/checksums before broad community distribution
